#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../@iqb/csv2ttl/dist_npm/addidcsv.js" "$@"
else 
  exec node  "$basedir/../@iqb/csv2ttl/dist_npm/addidcsv.js" "$@"
fi
